class Port {
    codeport;
    name;
    country;

    constructor(codeport, name, country) {
        this.codeport = codeport;
        this.name = name;
        this.country = country;
    }
}
export default Port;